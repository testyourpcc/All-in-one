services:
  web:
    build: .
    container_name: filetool_web
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    environment:
      PYTHONPATH: /code
    ports:
      - "8000:8000"
    volumes:
      - .:/code
